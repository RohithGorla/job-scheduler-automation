## Backend Deployment Note

The backend service is deployed on Render for demonstration.
Due to free-tier MySQL limitations, the database is configured
for local development.

All APIs, background jobs, and webhook integrations work
correctly when run locally, as documented above.
